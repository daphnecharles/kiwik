import { Signer } from 'ethers';
import { Provider, TransactionRequest } from '@ethersproject/providers';
import { ContractFactory, Overrides } from '@ethersproject/contracts';
import type { L1ERC20Gateway } from '../L1ERC20Gateway';
export declare class L1ERC20Gateway__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides): Promise<L1ERC20Gateway>;
    getDeployTransaction(overrides?: Overrides): TransactionRequest;
    attach(address: string): L1ERC20Gateway;
    connect(signer: Signer): L1ERC20Gateway__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): L1ERC20Gateway;
}
