import { Signer } from 'ethers';
import { Provider, TransactionRequest } from '@ethersproject/providers';
import { ContractFactory, PayableOverrides } from '@ethersproject/contracts';
import type { ConstructorCallback } from '../ConstructorCallback';
export declare class ConstructorCallback__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: PayableOverrides): Promise<ConstructorCallback>;
    getDeployTransaction(overrides?: PayableOverrides): TransactionRequest;
    attach(address: string): ConstructorCallback;
    connect(signer: Signer): ConstructorCallback__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): ConstructorCallback;
}
