import { Signer } from 'ethers';
import { Provider, TransactionRequest } from '@ethersproject/providers';
import { ContractFactory, Overrides } from '@ethersproject/contracts';
import type { BridgeUtils } from '../BridgeUtils';
export declare class BridgeUtils__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides): Promise<BridgeUtils>;
    getDeployTransaction(overrides?: Overrides): TransactionRequest;
    attach(address: string): BridgeUtils;
    connect(signer: Signer): BridgeUtils__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): BridgeUtils;
}
